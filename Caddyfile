# Static landing for Operia Term — served by Caddy on Railway ($PORT injected).
:{$PORT:80} {
	root * /srv
	encode gzip zstd
	file_server
	header {
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		Cache-Control "public, max-age=300"
	}
}
